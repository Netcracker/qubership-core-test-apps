name: Consul-login-test-service-go build

on:
  push:
    paths:
      - 'consul-login/test-service-go/**'
      - '.github/workflows/consul-login-test-service-go-on-commit.yaml'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'consul-login/test-service-go/**'
      - '.github/workflows/consul-login-test-service-go-on-commit.yaml'

jobs:
  build:
    uses: netcracker/qubership-core-infra/.github/workflows/generic-go-build.yaml@ed95ad948025728db1e613a3e1a4e6a11186f541 # v2.6.2
    with:
      go-module-dir: consul-login/test-service-go/test-service-go
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      ref: ${{ github.ref }}
      skip-docker: true
    secrets: inherit
