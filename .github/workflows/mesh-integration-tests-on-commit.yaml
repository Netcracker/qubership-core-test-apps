name: Mesh-integration-tests build

on:
  push:
    paths:
      - 'mesh-integration-tests/**'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'mesh-integration-tests/**'

jobs:
  mvn:
    uses: netcracker/qubership-core-infra/.github/workflows/maven-build-with-sonar.yaml@71206242f5967ac6691337913593f8623863f711 # v2.5.2
    with:
      event-name: ${{ github.event_name }}
      actor: ${{ github.actor }}
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      upload-artifact: false
      pom-file-path: mesh-integration-tests/pom.xml
    secrets:
      sonar-token: ${{ secrets.SONAR_TOKEN }}    
