name: Mesh-integration-tests build

on:
  push:
    paths:
      - 'mesh-integration-tests/**'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'mesh-integration-tests/**'

jobs:
  mvn:
    uses: netcracker/qubership-core-infra/.github/workflows/maven-build-with-sonar.yaml@8668020927f2a84d8d096a359c719c3c3a403520 # v2.5.0
    with:
      event-name: ${{ github.event_name }}
      actor: ${{ github.actor }}
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      upload-artifact: false
      pom-file-path: mesh-integration-tests/pom.xml
    secrets:
      sonar-token: ${{ secrets.SONAR_TOKEN }}    
