name: Consul-login-test-service-spring build

on:
  push:
    paths:
      - 'consul-login-test-service-spring/**'
      - '.github/consul-login-test-service-spring-docker-config.json'
      - '.github/workflows/consul-login-test-service-spring-on-commit.yaml'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'consul-login-test-service-spring/**'
      - '.github/consul-login-test-service-spring-docker-config.json'
      - '.github/workflows/consul-login-test-service-spring-on-commit.yaml'
jobs:
  build:
    uses: netcracker/qubership-core-infra/.github/workflows/generic-maven-build.yaml@e8322e6ee168795edec1d103e289b3fe3ceb805d # v2.6.0
    with:
      maven-command: 'verify'
      pom-file: consul-login-test-service-spring/pom.xml
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      ref: ${{ github.ref }}
      docker-config-file: consul-login-test-service-spring-docker-config.json
    secrets: inherit
