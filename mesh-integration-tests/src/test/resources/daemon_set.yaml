---
kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: {{name}}
spec:
  selector:
    matchLabels:
      name: {{label}}
  template:
    metadata:
      labels:
        name: {{label}}
      annotations:
        # placeholder workload, no mesh traffic: a sidecar only takes cpu away from the single test node
        sidecar.istio.io/inject: "false"
    spec:
      containers:
        - name: container
          image: {{image}}
          args:
            - sleep
            - infinity
          resources:
            limits:
              cpu: 100m
              memory: 10Mi
            requests:
              cpu: 10m
              memory: 10Mi
      securityContext:
        runAsUser: 1000
      nodeSelector:
        kubernetes.io/hostname: {{node}}